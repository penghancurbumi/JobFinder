# Token Cloudflare Tunnel (WAJIB diisi sebelum `docker compose up`)
# Ambil dari: https://one.dash.cloudflare.com > Networks > Tunnels > Create a tunnel
# > pilih "Docker" > copy token panjang di contoh perintah `cloudflared service install ...`
TUNNEL_TOKEN=isi-token-cloudflare-tunnel-di-sini
